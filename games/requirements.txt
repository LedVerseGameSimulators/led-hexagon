# Ledhexagon Clone - Dependencies
# Managed with UV. Run: uv pip install -r requirements.txt
# Python >= 3.11 required (UV manages the Python install)
#
# On Windows (production): uv venv --python 3.12 && uv pip install -r requirements.txt
# On macOS (dev/test):     uv venv --python 3.12 && uv pip install -r requirements.txt

pygame>=2.0.1
Pillow>=9.0.0
pyserial>=3.5
loguru>=0.5.3
pycryptodome>=3.15.0
rsa>=4.8
pynput>=1.7.6
moviepy>=1.0.3
imageio>=2.9.0
imageio-ffmpeg>=0.4.5
tqdm>=4.64.0
proglog>=0.1.10
colorama>=0.4.5
mysql-connector-python>=8.0.28
opencv-python>=4.5.0

# Web simulator
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
